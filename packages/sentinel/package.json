{
  "name": "@aegis/sentinel",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime detection and governance platform for AI agent security",
  "scripts": {
    "build": "npm run build:gateway && npm run build:mcp-proxy",
    "build:gateway": "cd services/gateway && npx tsc",
    "build:mcp-proxy": "cd services/mcp-proxy && npx tsc",
    "clean": "rm -rf services/gateway/dist services/mcp-proxy/dist"
  },
  "dependencies": {
    "@aegis/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@noble/ed25519": "^2.2.3",
    "commander": "^13.1.0",
    "express": "^4.21.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.8.0"
  }
}
