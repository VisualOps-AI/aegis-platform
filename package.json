{
  "name": "aegis-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "packageManager": "npm@11.6.2",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
