# Proof Assets

Drop the two MVP proof screenshots in this folder using the exact filenames below.
The README references them by these paths, so the names must match.

## Required files

| Filename | What to capture |
|---|---|
| `cli-scan-screenshot.png` | A terminal showing `aegis scan` running against an MCP config and printing the risk-score summary box (Critical / High / Medium / Total / Duration). |
| `test-output-screenshot.png` | A terminal showing the test suite passing (`pnpm test` / `vitest run`) with the green pass summary. |

## Notes

- Keep images reasonably sized (≈ 1200–1800px wide). Crop to the relevant terminal region.
- PNG only. Do not rename — the root `README.md` links to these exact paths.
- These are placeholders until the real captures are added; the section renders broken image
  icons in the meantime, which is expected.
